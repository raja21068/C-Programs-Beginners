#include"conio.h"
#include"stdio.h"

void main(void)
{
 int Q[5],a;
 clrscr();

 for(a=0; a<=4; a++)
 {
  printf("Enter values = ");
  scanf("%d",Q[a]);
  }

  for(a=0; a<=4; a++)
  {
   if(Q[a]%2==0);
   printf("%d\n",Q[a]);
  }
  getch();


}